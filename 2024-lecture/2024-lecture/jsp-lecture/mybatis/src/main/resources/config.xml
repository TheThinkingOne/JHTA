<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties"></properties>
    <typeAliases>
        <typeAlias alias="BoardDto"  type="com.leekiye.mybatis.dto.BoardDto" />
        <typeAlias alias="PageDto"   type="com.leekiye.mybatis.dto.PageDto" />
        <typeAlias alias="SearchDto" type="com.leekiye.mybatis.dto.SearchDto" />
        <typeAlias alias="MemberDto" type="com.leekiye.mybatis.dto.MemberDto" />
    </typeAliases>

    <!-- typeHandlers : 자바 타입이랑 DB 타입을 맞춰줘야 할 때 사용 -->
    <typeHandlers>
        <typeHandler handler="com.leekiye.mybatis.mybatis.GradeTypeHandler"></typeHandler>
    </typeHandlers>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="board.xml"/>
        <mapper resource="member.xml"/>
    </mappers>
</configuration>
